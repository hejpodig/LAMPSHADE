# Streamlit UI
streamlit>=1.31
streamlit-clickable-images

# Core deps used by the app and FullControl
plotly
pydantic
numpy

# Install FullControl from the local source folder (non-editable is more reliable on Streamlit Cloud)
./fullcontrol
